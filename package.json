{
  "name": "address-validation-server",
  "version": "1.0.0",
  "description": "Address validation API proxy for e-commerce checkouts, backed by the Google Address Validation API",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.0"
  }
}
